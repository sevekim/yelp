import 'package:flutter/material.dart';

/// **********************************************************************
///
///  Left Top Right only
///
/// **********************************************************************/

const commonRightPadding2 = EdgeInsets.only(
  right: 2.0,
);

const commonRightPadding4 = EdgeInsets.only(
  right: 4.0,
);

const commonRightPadding5 = EdgeInsets.only(
  right: 5.0,
);

const commonRightPadding6 = EdgeInsets.only(
  right: 6.0,
);

const commonRightPadding8 = EdgeInsets.only(
  right: 8.0,
);

const commonRightPadding10 = EdgeInsets.only(
  right: 10.0,
);

const commonRightPadding11 = EdgeInsets.only(
  right: 11.0,
);

const commonRightPadding12 = EdgeInsets.only(
  right: 12.0,
);

const commonRightPadding13 = EdgeInsets.only(
  right: 13.0,
);

const commonRightPadding16 = EdgeInsets.only(
  right: 16.0,
);
