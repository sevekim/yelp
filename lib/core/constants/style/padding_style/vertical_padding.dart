import 'package:flutter/material.dart';

/// **********************************************************************
///
///  Input field prefix icon padding
///
/// **********************************************************************/

const commonVerticalPadding4 = EdgeInsets.symmetric(
  vertical: 4.0,
);

const commonVerticalPadding6 = EdgeInsets.symmetric(
  vertical: 6.0,
);

const commonVerticalPadding8 = EdgeInsets.symmetric(
  vertical: 8.0,
);

const commonVerticalPadding12 = EdgeInsets.symmetric(
  vertical: 12.0,
);

const commonVerticalPadding16 = EdgeInsets.symmetric(
  vertical: 16.0,
);

const commonVerticalPadding24 = EdgeInsets.symmetric(
  vertical: 24.0,
);

const commonVerticalPadding60 = EdgeInsets.symmetric(
  vertical: 60.0,
);

const commonVerticalPaddingTop70Bot39 = EdgeInsets.only(
  top: 70,
  bottom: 39,
);
const commonVerticalPaddingTop48Bot16 = EdgeInsets.only(
  top: 48,
  bottom: 16,
);
