import 'package:flutter/material.dart';

/// **********************************************************************
///
///  General Bottom padding
///
/// **********************************************************************/

const commonBottomPadding2 = EdgeInsets.only(
  bottom: 2.0,
);

const commonBottomPadding4 = EdgeInsets.only(
  bottom: 4.0,
);

const commonBottomPadding6 = EdgeInsets.only(
  bottom: 6.0,
);

const commonBottomPadding7 = EdgeInsets.only(
  bottom: 7.0,
);

const commonBottomPadding8 = EdgeInsets.only(
  bottom: 8.0,
);

const commonBottomPadding10 = EdgeInsets.only(
  bottom: 10.0,
);

const commonBottomPadding12 = EdgeInsets.only(
  bottom: 12.0,
);

const commonBottomPadding16 = EdgeInsets.only(
  bottom: 16.0,
);

const commonBottomPadding18 = EdgeInsets.only(
  bottom: 18.0,
);

const commonBottomPadding20 = EdgeInsets.only(
  bottom: 20.0,
);

const commonBottomPadding22 = EdgeInsets.only(
  bottom: 22.0,
);

const commonBottomPadding24 = EdgeInsets.only(
  bottom: 24.0,
);

const commonBottomPadding28 = EdgeInsets.only(
  bottom: 28.0,
);

const commonBottomPadding32 = EdgeInsets.only(
  bottom: 32.0,
);

const commonBottomPadding35 = EdgeInsets.only(
  bottom: 35.0,
);

const commonBottomPadding40 = EdgeInsets.only(
  bottom: 40.0,
);

const commonBottomPadding70 = EdgeInsets.only(
  bottom: 70.0,
);
