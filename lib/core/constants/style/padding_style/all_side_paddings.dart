import 'package:flutter/material.dart';

/// **********************************************************************
///
///  Scaffold All padding
///
/// **********************************************************************/

const commonNoPadding = EdgeInsets.all(0);

const commonAllSidePadding4 = EdgeInsets.all(4);

const commonAllSidePadding8 = EdgeInsets.all(8);

const commonAllSidePadding12 = EdgeInsets.all(
  12.0,
);

const commonAllSidePadding16 = EdgeInsets.all(
  16.0,
);

const commonAllSideH8V4 = EdgeInsets.symmetric(
  horizontal: 8.0,
  vertical: 4.0,
);

const commonAllSidePadding14Bottom8 = EdgeInsets.fromLTRB(14, 14, 14, 8);
const commonAllSidePadding16Bottom8 = EdgeInsets.fromLTRB(16, 16, 16, 80);

const commonHorizontal6Vertical12 = EdgeInsets.symmetric(
  horizontal: 12,
  vertical: 6,
);

const commonHorizontal6Vertical2 = EdgeInsets.symmetric(
  horizontal: 6,
  vertical: 2,
);

const commonHorizontal16Vertical8 = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 8,
);

const commonHorizontal16Vertical24 = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 24,
);

const commonHorizontal16Vertical60 = EdgeInsets.symmetric(
  horizontal: 16,
  vertical: 60,
);

const commonLTR16B80 = EdgeInsets.only(
  top: 16,
  left: 16,
  right: 16,
  bottom: 80,
);

const commonLTR16B8 = EdgeInsets.only(
  top: 16,
  left: 16,
  right: 16,
  bottom: 8,
);

const commonLBR16T2 = EdgeInsets.only(
  top: 2,
  left: 16,
  right: 16,
  bottom: 16,
);

const commonHorizontal14Vertical6 = EdgeInsets.symmetric(
  horizontal: 14,
  vertical: 6,
);
