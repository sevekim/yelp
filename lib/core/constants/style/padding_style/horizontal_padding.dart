import 'package:flutter/material.dart';

/// **********************************************************************
///
///  Input field prefix icon padding
///
/// **********************************************************************/

const commonLeft12Right8Padding = EdgeInsets.only(
  left: 12.0,
  right: 8.0,
);

const commonLeft8Right12Padding = EdgeInsets.only(
  left: 8.0,
  right: 12.0,
);

const commonLeft16Right8Padding = EdgeInsets.only(
  left: 16.0,
  right: 8.0,
);

const commonHorizontalPadding12dot5 = EdgeInsets.symmetric(
  horizontal: 12.5,
);

const commonHorizontalPadding3dot5 = EdgeInsets.symmetric(
  horizontal: 3.5,
);

const commonHorizontalPadding8 = EdgeInsets.symmetric(
  horizontal: 8.0,
);

const commonHorizontalPadding15 = EdgeInsets.symmetric(
  horizontal: 15.0,
);

const commonHorizontalPadding16 = EdgeInsets.symmetric(
  horizontal: 16.0,
);
